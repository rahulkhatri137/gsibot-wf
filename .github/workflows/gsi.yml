name: Building GSI..

on: [push]

env:
  ROM_URL: https://master.dl.sourceforge.net/project/sgsi137/Nubia/RedMagicOS-4.5%20-%2011%20-%20NX669J/RedMagicOS-4.5-full-AB-11-20220220-NX669J-SGSI137.img.gz?viasf=1
  ROM_NAME: RedMagic
  ZIP_NAME: RedMagicOS-4.5
  TZ: Asia/Kolkata
jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
       - name: Checkout
         uses: actions/checkout@master
           
       - name: Initializing environment
         run: |
            sudo -E apt-get -qq update
            sudo -E apt-get -qq install git aria2 simg2img expect

       
       - name: Clone Debloat tool
         run: git clone --recurse-submodules https://github.com/rahulkhatri137/Debloat -b main
       
       - name: Setup
         run: |
              sudo chmod -R 777 Debloat
              cd Debloat
              sudo bash setup.sh
       
       - name: Download & Debloat GSI
         run: cd Debloat && sudo ./url2Debloat.sh $ROM_URL $ROM_NAME
       
       - name: GZip
         run: |
              sudo chmod -R 777 Debloat/output
              cd Debloat/output
              ls
              sudo chmod -R 777 *.img
              gzip -fq9 *.img ; rm -rf *.img
              ls
       
       - name: Upload 
         run: |
              cd Debloat/output
              ls
              expect -c " 
              spawn sftp ${{ secrets.SFUSER }}@frs.sourceforge.net
              expect \"yes/no\"
              send \"yes\r\"
              expect \"Password\"        
              send \"${{ secrets.SFPASS }}\r\"
              expect \"sftp> \"
              send \"cd ${{ secrets.SFDIR }}\r\"
              set timeout -1
              send \"mkdir $ZIP_NAME\r\"
              expect \"sftp> \"
              send \"cd $ZIP_NAME\r\"
              set timeout -1
              send \"put *\r\"
              expect \"Uploading\"
              expect \"100%\"
              expect \"sftp>\"
              send \"bye\r\"
              interact"
